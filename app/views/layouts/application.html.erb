<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Amaha</title>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>

  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
      min-height: 100vh;
      background-color: #f8f9fa;
    }
    .container {
      width: 100%;
      max-width: 800px;
      padding: 20px;
      box-sizing: border-box;
      margin-left: 18%;
    }
    .notice, .alert {
      padding: 10px;
      margin-bottom: 20px;
      border-radius: 5px;
      width: 100%;
      box-sizing: border-box;
    }
  
    
    .btn {
      display: inline-block;
      padding: 10px 20px;
      margin: 5px 0;
      font-size: 16px;
      color: white;
      background-color: #007bff;
      border: none;
      border-radius: 4px;
      text-align: center;
      text-decoration: none;
      cursor: pointer;
    }
    .btn:hover {
      background-color: #0056b3;
    }
    .profile-links {
      display: flex;
      justify-content: space-between;
      margin-bottom: 20px;
    }
    .btn:hover {
      background-color: #0056b3;
    }
    .btn.btn-danger {
      background-color: #dc3545; /* Red color for logout */
    }
    .btn.btn-danger:hover {
      background-color: #c82333; /* Darker red on hover */
    }
    .btn.btn-success {
      background-color: #28a745; /* Green color for profile */
    }
    .btn.btn-success:hover {
      background-color: #218838; /* Darker green on hover */
    }
    @media (max-width: 600px) {
      .profile-links {
        flex-direction: column;
        align-items: center;
      }
      .profile-links a, .profile-links form {
        width: 100%;
        text-align: center;
        margin: 5px 0;
      }
    }
    
  </style>
</head>
<body>

  <div class="container">
  <p class="notice"> <%= notice %> </p>
  <p class="alert"> <%= alert %> </p>

    <% if user_signed_in? %>
    <% if current_user.profile_picture.attached? %>
      <%= image_tag(current_user.profile_picture,style:"border-radius: 50%;width:50px;display: inline-block;height: 50px;") %>
    <% end %>
      <div class="profile-links">
        <%= link_to "Profile", edit_user_registration_path, class: "btn btn-success" %>
        <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "btn btn-danger" %>
      </div>
    <% end %>

    <%= yield %>
  </div>
</body>
</html>
