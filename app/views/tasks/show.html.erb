<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Task Details</title>
  <style>
    body {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      background-color: #f8f9fa;
      margin: 0;
    }
    .task-details {
      background-color: #ffffff;
      padding: 20px;
      border-radius: 5px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      width: 100%;
      max-width: 600px;
      margin-left: 18%;
      box-sizing: border-box;
    }
    .task-details h2 {
      text-align: center;
      margin-bottom: 20px;
    }
    .task-details p {
      color: green;
      text-align: center;
      margin-bottom: 10px;
    }
    .task-details .actions {
      margin-top: 20px;
      text-align: center;
    }
    .task-details .actions a,
    .task-details .actions button {
      margin-right: 10px;
    }
  </style>
</head>
<body>

<div class="task-details">
  <% if notice.present? %>
    <p><%= notice %></p>
  <% end %>

  <%= render @task %>

  <div class="task-details-header">
    <%= button_to 'Add to Google Calendar', add_to_google_calendar_task_path(@task), method: :post, class: "btn btn-success" %>
    <%= button_to "Destroy this task", @task, method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-danger" %>
  </div>
</div>

</body>
</html>
